# flowercard — 프로젝트 브리프

## 0. Claude Code에게 (작업 규칙)

- **모든 설명은 한국어로.** 사용자는 한국어 사용자다.
- **사용자는 프로그래밍 완전 초보자다.** 전문 용어(저장소, 커밋, 푸시, 배포, 빌드, 환경변수 등)가 나올 때마다 짧게 풀어서 설명할 것. 설명 없이 용어만 던지지 말 것.
- **사용자는 주로 휴대폰에서 Claude 앱의 Code 탭으로 작업한다.** 그래서 사용자가 직접 손으로 해야 하는 일(웹사이트 가입, 버튼 클릭, 값 복사 등)이 생기면 **반드시 작업을 멈추고**, 무엇을 어디서 눌러야 하는지 하나씩 알려줄 것. 알아서 진행하지 말 것.
- **한 번에 한 단계씩.** 여러 단계를 몰아서 처리하지 말고, 한 단계 끝나면 사용자가 확인할 수 있게 멈출 것.
- 코드를 짜기 전에 **먼저 계획을 쉬운 말로 설명**하고 사용자 확인을 받을 것.
- 사용자가 좌절하지 않게, 잘 되고 있다는 걸 중간중간 알려줄 것.

---

## 1. 만들려는 것

친구들이 **각자 자기 폰이나 컴퓨터에서 링크로 접속해** 함께 하는 **턴제(순서가 한 명씩 돌아가는) 파티 게임**.

- 같은 화면을 돌려쓰는 방식이 **아님**. 각자 기기에서 접속.
- "방 만들기 → 방 코드 공유 → 친구가 코드로 참가" 구조.
- 최종 목표: **머리가 꽃밭인 사람들의 서열 카드게임** (`flowercard`)
- 하지만 **첫 결과물은 틱택토(3목)** 로 시작한다. 이유는 아래 4번 참고.

---

## 2. 확정된 기술 선택

| 항목 | 선택 | 이유 |
|---|---|---|
| 실시간 동기화 | **Playroom Kit** | 서버 코드를 한 줄도 안 짜도 방 생성·방 코드·링크·QR이 자동으로 됨 |
| 파일 구성 | **HTML / CSS / JS만** (프레임워크 없이 시작) | 초보자가 이해할 수 있는 최소 구조 |
| 서버 | **직접 만들지 않음** | 초보자 최대 함정 회피 |
| 배포 | **Vercel 또는 Netlify** (정적 배포) | 무료, GitHub 연결 시 자동 배포 |
| 코드 저장소 | GitHub `flowercard` | Claude Code 클라우드 세션에 필요 |

### 왜 서버를 안 만드는가 (중요)

직접 WebSocket 서버를 만들면 초보자가 다음 함정에 전부 걸린다:

- 로컬에서 `ws://localhost`로 되던 게 배포하면 `wss://`가 아니라서 안 됨
- Vercel/Netlify 무료 플랜은 **WebSocket 서버를 못 돌림** (서버리스 함수 방식이라 연결을 계속 유지 못 함)
- Render 무료 플랜은 15분 놀면 잠들고 다시 켜지는 데 1분쯤 걸림 → 친구들과 놀기에 최악
- 환경변수(.env)가 배포 서버에 자동으로 안 따라가서 "로컬은 되는데 배포하면 크래시"

Playroom Kit을 쓰면 이 문제들이 **통째로 사라진다.** 실시간 처리를 외부가 해주므로, 게임 파일은 그냥 정적 사이트에 올리면 된다.

---

## 3. Playroom Kit 핵심 API

```
insertCoin()      // 게임 시작. 방 생성 + 방 코드 + QR + 공유 링크가 자동 생성됨
onPlayerJoin()    // 플레이어 참가 감지
myPlayer()        // 내 플레이어 정보
setState() / getState()   // 모두가 공유하는 상태 읽기·쓰기
getRoomCode()     // 방 코드 가져오기
RPC               // 원격 호출
```

- 무료 티어: **하루 최대 10명** (신용카드 불필요). 친구 몇 명과 테스트하기엔 충분.
- 유료 Lite: 월 $10 / 월 1만 MAU
- `dev.joinplayroom.com`에서 게임을 등록하고 **gameId**를 받아야 함 → 이건 사용자가 직접 해야 하는 일이므로 그 시점에 멈추고 안내할 것.

---

## 4. 왜 틱택토부터인가

턴제가 실시간 액션보다 훨씬 쉬운 이유:

1. **통신 횟수**: 턴제는 "한 수 뒀어" 한 번만 보내면 됨. 액션은 초당 수십 번.
2. **지연 허용치**: 턴제는 0.5초 늦어도 아무도 모름. 액션은 0.1초만 늦어도 게임이 깨짐.
3. **동기화 기법**: 액션은 예측(prediction)·보간(interpolation) 같은 고급 기법 필요. 턴제는 현재 상태만 공유하면 끝.

틱택토는 그중에서도 가장 단순해서, **Playroom 연결이 제대로 되는지 확인하는 시험대**로 딱 맞다. 이게 친구 폰에서 실제로 돌아가는 걸 확인한 다음에 서열 카드게임으로 넘어간다.

---

## 5. 단계별 로드맵

**1단계 — 틱택토 완성 (지금 여기)**
- Playroom Kit 연결
- 방 만들기 / 방 코드로 참가
- 두 명이 번갈아 두기, 승패 판정
- 내 컴퓨터·폰에서 창 2개로 테스트

**2단계 — 배포**
- Vercel 또는 Netlify에 올려서 `https://` 링크 만들기
- GitHub에 푸시하면 자동 배포되게 연결

**3단계 — 친구와 실전 테스트**
- 링크 공유 → 실제로 각자 폰에서 접속해서 플레이
- 안 되는 부분 찾아서 고치기

**4단계 — 서열 카드게임으로 확장**
- 카드 덱, 나눠주기, 턴 순서, 서열 비교 규칙
- 3~6명 지원
- 점수판

---

## 6. 반드시 피해야 할 함정

- **상태 충돌**: 여러 명이 동시에 같은 값을 고치면 깨진다. → **호스트(방장) 한 명만 게임 상태를 바꾸고, 나머지는 받아서 화면만 갱신**하는 구조로 만들 것.
- **비밀키 노출**: `.env` 파일은 절대 GitHub에 올리지 말 것. `.gitignore`에 반드시 추가.
- **빌드 후 값 고정**: 환경변수를 바꿨으면 반드시 다시 배포해야 반영됨.
- **모바일 화면**: 친구들이 폰으로 접속하므로 처음부터 세로 화면·터치 기준으로 만들 것. 나중에 고치면 훨씬 힘들다.

---

## 7. 첫 작업 지시

위 내용을 이해했으면:

1. 이 프로젝트의 계획을 **쉬운 한국어로 요약**해서 사용자에게 보여줄 것
2. 사용자가 직접 해야 할 첫 번째 일이 뭔지 알려줄 것 (Playroom gameId 발급 등)
3. 확인을 받은 뒤에 틱택토 코드 작성 시작

**아직 코드를 짜지 말 것. 계획 설명부터.**
